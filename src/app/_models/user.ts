// Not used at the moment,
// if at some point it is necessary this will included the class user
// The values below are just an example, and the real User class would be
// different
export class User {
    //id:number;
    username: string;
    password: string;
    email:string;
    unit:string;
    access:string;
    token?: string;
}
